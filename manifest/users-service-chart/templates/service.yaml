apiVersion: v1
kind: Service
metadata:
  name: {{ include "users-service-chart.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "users-service-chart.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
